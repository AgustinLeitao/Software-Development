global:
  environment: prod

backend:
  replicaCount: 3

frontend:
  replicaCount: 2

ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: www.todo-app.lat
  tls:
    - hosts:
        - www.todo-app.lat
      secretName: todo-app-tls

